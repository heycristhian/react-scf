const headers = {
    Authorization: `Bearer ksakodkoskdo`,
    'Content-Type': 'application/json',
  };

  const API_PATH =
  process.env.REACT_APP_API_PATH || 'https://api.domrock.ai/armen/dev';

  const postLogin = () => {

  }